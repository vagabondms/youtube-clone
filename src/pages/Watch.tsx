import Layout from '@components/Layout';

import WatchContentWrapper from '@components/WatchContentWrapper';

const Video = () => (
  <Layout>
    <WatchContentWrapper />
  </Layout>
);

export default Video;
